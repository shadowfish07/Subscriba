import { StyleSheet } from "react-native";

export const globalStyles = StyleSheet.create({
  card: {
    marginBottom: 10,
    marginLeft: 15,
    marginRight: 15,
  },
  textInput: {
    marginBottom: 10,
  },
  buttonInCard: {
    marginTop: 20,
  },
});
